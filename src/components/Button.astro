---

const { icon, link, arrowIcon = false } = Astro.props;


---
<a href={link} class:list={["standard-button", { arrowIcon: arrowIcon }]}>
    <span class="content">
        <slot />
    </span>
    {icon && <img src={icon} alt="" class="icon" />}
</a>
