// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://oscar-kristensson.github.io',
  base: 'Portfolio', // omit if using a custom domain or user site
});